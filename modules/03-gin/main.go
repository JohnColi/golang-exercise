package main

import "fmt"

func main() {
	fmt.Println("=== Module 3: Gin + RESTful API ===")

	// TODO: 建立 Gin engine、掛上路由、Listen
	// 提示：完成 routes.go 的 setupRouter() 後取消註解
	// r := setupRouter()
	// if err := r.Run(":8080"); err != nil {
	// 	panic(err)
	// }
}
